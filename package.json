{
  "name": "woton",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "b": "npm --filter @woton/backend",
    "w": "npm --filter @woton/webapp",
    "dev": "npm -r --parallel dev",
    "types": "concurrently 'npm b types' 'npm f types'",
    "prettify": "npm -r --parallel prettify"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.0.2",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.6"
  }
}
